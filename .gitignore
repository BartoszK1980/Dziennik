# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Build / deps (na wyrost — gdyby kiedyś doszedł frontend)
node_modules/
dist/
build/
.env
.env.local

# Skill credentials (zawiera sb_secret_* — nigdy nie commitować)
.claude/skills/*/credentials.local.json

# Lokalne dane importu / seed (jednorazowe skrypty i eksporty wpisów)
batch_*.sql
insert_*.sql
entries_*.txt
entries_all.json
