{
  "name": "dziennik",
  "version": "1.0.0",
  "private": true,
  "description": "Dziennik — static frontend + Vercel serverless MCP server",
  "dependencies": {
    "@vercel/node": "^3.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
